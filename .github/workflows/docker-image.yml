name: Build and Push Docker Image

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.jeambatite }}
          password: ${{ secrets.dckr_pat_LtX4a3B439dquVl4fwwZYOpnQ6I }}

      - name: Build Docker image
        run: docker build -t ${{ secrets.jeambatite }}/miapp:latest .

      - name: Push Docker image
        run: docker push ${{ secrets.jeambatite }}/miapp:latest
